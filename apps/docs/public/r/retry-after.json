{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "retry-after",
  "type": "registry:ui",
  "title": "Retry After",
  "description": "A rate limit countdown that gates retry until it clears.",
  "dependencies": [
    "@gear5/core"
  ],
  "registryDependencies": [],
  "files": [
    {
      "path": "components/ui/retry-after.tsx",
      "type": "registry:ui",
      "target": "components/ui/retry-after.tsx",
      "content": "\"use client\";\n\nimport * as React from \"react\";\nimport {\n  RetryAfter as RetryAfterPrimitive,\n  RetryAfterButton,\n  RetryAfterMessage,\n} from \"@gear5/core\";\nimport type { RetryAfterProps as RetryAfterPrimitiveProps } from \"@gear5/core\";\nimport { cn } from \"./lib/utils\";\n\n/** Styled rate limit wait. */\nexport function RetryAfter({ className, ...props }: RetryAfterPrimitiveProps) {\n  return (\n    <RetryAfterPrimitive\n      className={cn(\n        \"flex flex-wrap items-center gap-3 rounded-panel border px-3 py-2.5\",\n        \"border-warning/40 bg-warning/[0.06]\",\n        \"data-[ready]:border-line data-[ready]:bg-panel\",\n        className,\n      )}\n      {...props}\n    >\n      <RetryAfterMessage className=\"min-w-0 flex-1 text-[13px] text-fg-muted\" />\n      <RetryAfterButton\n        className={cn(\n          \"shrink-0 rounded-chip border border-line px-2.5 py-1 text-[12px] transition-colors\",\n          \"text-fg-muted hover:border-line-strong hover:text-fg\",\n          \"disabled:pointer-events-none disabled:opacity-40\",\n          \"data-[ready]:border-accent data-[ready]:text-accent\",\n        )}\n      />\n    </RetryAfterPrimitive>\n  );\n}\n"
    },
    {
      "path": "lib/utils.ts",
      "type": "registry:lib",
      "target": "lib/utils.ts",
      "content": "import { clsx } from \"clsx\";\nimport type { ClassValue } from \"clsx\";\nimport { twMerge } from \"tailwind-merge\";\n\n/** Merge class names, letting a caller's utility win over the default. */\nexport function cn(...inputs: ClassValue[]) {\n  return twMerge(clsx(inputs));\n}\n"
    }
  ]
}
